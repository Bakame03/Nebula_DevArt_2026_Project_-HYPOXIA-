// Twitter card reuses the same generated card as OpenGraph. The existing
// `twitter.card: "summary_large_image"` in layout.tsx pairs with this image.
export { default, alt, size, contentType } from "./opengraph-image";
